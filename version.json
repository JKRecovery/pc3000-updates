{
  "version": "1.0.226",
  "notes": "Version 1.0.226 — fixes a bug when saving the client-selection (acerep) report as ZIP.",
  "changelog": [
    "Fixed: saving the client-selection report (acerep panel) as ZIP could save the file with a \".html\" name instead of \".zip\", even though the file type was correctly set to ZIP in the save dialog."
  ]
}